// Objective: convert a number from 0 to 999 into words
public class ThreeDigitConversion {
  public static String convertThreeDigits(int number) {
    
    // base case
    if (number == 0) {
        return "";
    }
    
    // case 1: numbers < 20
    
    // case 2: number < 100

    // case 3: number >= 100


    return "";
  } 
}
